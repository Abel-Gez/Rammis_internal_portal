import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

   // disable the bottom-left Next.js overlay menu in dev
   devIndicators: false,

};

export default nextConfig;
